# async-cpp-logger
C++17 简易异步日志库，练手项目，生产者消费者模型。

# 功能
- 异步写文件，业务线程只把消息丢入队列，写盘交给后台线程
- 可变参数模板，支持 `{}` 占位符格式化
- 互斥锁 + 条件变量实现线程安全阻塞队列
- 程序析构时自动等待剩余日志写完，不会丢日志
- 增加时间戳功能
- 为日志消息添加不同的级别 
# 编译
```bash
g++ Logger.cpp -o logger -std=c++17 -pthread
./logger
